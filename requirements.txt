# AI Employee Gold Tier — Python Dependencies
# Install with: pip install -r requirements.txt
# Or with uv:   uv add -r requirements.txt

# File system watcher (required for FileSystemWatcher)
watchdog>=4.0.0

# Gmail Watcher (Silver Tier)
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.0.0

# LinkedIn Watcher — API library, no browser required (Silver Tier)
linkedin-api>=2.0.0

# Facebook Watcher — HTTP requests with session cookies (Gold Tier)
requests>=2.31.0

# Instagram Watcher — API library, no browser required (Gold Tier)
instagrapi>=2.0.0

# Twitter/X Watcher — API library, no browser required (Gold Tier)
tweepy>=4.14.0

# Scheduling / orchestration
schedule>=1.2.0

# Optional: Environment variable loading
python-dotenv>=1.0.0
